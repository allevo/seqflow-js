{
  "name": "seqflow-js-root",
  "version": "0.0.1-beta.1",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "turbo run --log-order=stream build",
    "test": "turbo run --log-order=stream test",
    "biome:check": "turbo run biome:check",
    "postinstall": "rm -rf node_modules/@types/react && rm -rf node_modules/.pnpm/@types+react*"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "turbo": "^2.2.3"
  },
  "devDependencies": {
    "execa": "^9.5.1"
  }
}
